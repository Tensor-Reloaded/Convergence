defaults:
  - common: default
  - model: BasicConvMnist
  - dataset: mnist_01
  - optimizer: sgd
  - initializer: default
  - save: default
  - scheduler: milestones
  - specific: baseline
  - dataset_model_specific: ${defaults.2.dataset}/${defaults.7.specific}/${defaults.1.model}
    optional: true

model: ${model_name}(${model_depth})
dataset: ${dataset_name}
scheduler: ${scheduler_name}
save_dir: "${model}/${aux_save_dir}/${seed}"

#load_model: "C:/Facultate/Tensor-Reloaded/storage/PreResNet(depth=20, in_channels=1, out_classes=2)/Baseline/0/model_0_0.0.pth"

load_model: "C:/Facultate/Tensor-Reloaded/storage/BasicConv(in_channels=1, out_classes=2)/Baseline/0/model_0_0.0.pth"

#load_model: ""

all_batch_combinations: True